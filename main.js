// This action uses pre/post hooks, so the main step is intentionally empty
// The actual build happens in the workflow between pre and post
console.log('VCPerf trace is active - build your project now');
